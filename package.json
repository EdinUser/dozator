{
  "name": "dozator",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "screenshots:docs": "node scripts/docs-screenshots.mjs",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run build && npm run test:e2e"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "^5.3.3",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
